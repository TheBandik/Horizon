services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: horizon_dev
      POSTGRES_USER: horizon
      POSTGRES_PASSWORD: devpass
    ports:
      - "5432:5432"
    volumes:
      - horizon_dev_db:/var/lib/postgresql/data

volumes:
  horizon_dev_db:
